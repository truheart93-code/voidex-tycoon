import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.truheart.voidextycoon',
  appName: 'VoidEx Tycoon',
  webDir: 'dist'
};

export default config;
